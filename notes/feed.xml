<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
  <title>Notes — Brahim Bousnguar</title>
  <subtitle>MuleSoft, SAP Commerce, Salesforce integration and AI-augmented engineering.</subtitle>
  <link rel="self" type="application/atom+xml" href="https://heybrahim.com/notes/feed.xml"/>
  <link rel="alternate" type="text/html" href="https://heybrahim.com/notes/"/>
  <id>https://heybrahim.com/notes/</id>
  <updated>2026-09-18T00:00:00Z</updated>
  <author><name>Brahim Bousnguar</name><uri>https://heybrahim.com/</uri></author>
  <entry>
    <title>Reading MuleSoft Anypoint logs from an AI agent: why I built mulewatch</title>
    <link rel="alternate" type="text/html" href="https://heybrahim.com/notes/mulewatch-anypoint-logs-mcp.html"/>
    <id>https://heybrahim.com/notes/mulewatch-anypoint-logs-mcp.html</id>
    <published>2026-09-18T00:00:00Z</published>
    <updated>2026-09-18T00:00:00Z</updated>
    <summary>MuleSoft&#x27;s official MCP server builds and deploys Mule apps but cannot read application logs. mulewatch is a read-only MCP server that fetches live and archived Anypoint logs, finding the right replica in a handful of calls.</summary>
    <category term="MuleSoft"/><category term="Anypoint Platform"/><category term="MCP"/><category term="observability"/>
    <content type="html">&lt;p&gt;&lt;strong&gt;TL;DR&lt;/strong&gt; — MuleSoft ships an official MCP server, but it is a development tool: it scaffolds, generates and deploys, and it does not retrieve application logs. During an incident, logs are the first thing you need. &lt;a href=&quot;https://github.com/brbousnguar/mulewatch&quot; target=&quot;_blank&quot; rel=&quot;noopener noreferrer&quot;&gt;mulewatch&lt;/a&gt; is an open-source, read-only MCP server that gives Claude, Cursor or any MCP client the live Runtime Manager logs &lt;em&gt;and&lt;/em&gt; the historical Monitoring Archive — resolving the right replica in a few calls instead of hundreds. It runs with &lt;code&gt;npx -y mulewatch&lt;/code&gt; and your own connected app.&lt;/p&gt;
&lt;h2 id=&quot;what-does-the-official-mulesoft-mcp-server-cover&quot;&gt;What does the official MuleSoft MCP server cover?&lt;/h2&gt;
&lt;p&gt;MuleSoft publishes &lt;a href=&quot;https://docs.mulesoft.com/mulesoft-mcp-server&quot; target=&quot;_blank&quot; rel=&quot;noopener noreferrer&quot;&gt;&lt;code&gt;mulesoft-mcp-server&lt;/code&gt;&lt;/a&gt;. It is built for the person &lt;em&gt;writing&lt;/em&gt; the integration: it creates Mule projects, generates flows and API specs, and deploys applications, mostly from a local project open in an IDE.&lt;/p&gt;
&lt;p&gt;That is a good tool for building. It is the wrong shape for operating. At the time of writing (September 2026) it has no tool for retrieving or searching application logs, and it assumes a checked-out Mule project that the on-call engineer usually doesn&#x27;t have.&lt;/p&gt;
&lt;h2 id=&quot;why-are-logs-the-part-that-matters&quot;&gt;Why are logs the part that matters?&lt;/h2&gt;
&lt;p&gt;When an order stops reaching the ERP at 2 a.m., the questions are always the same:&lt;/p&gt;
&lt;ul&gt;&lt;li&gt;Which application failed, in which environment?&lt;/li&gt;&lt;li&gt;What did it log around the time of the failure?&lt;/li&gt;&lt;li&gt;Did it fail once, or has it been failing since the last deployment?&lt;/li&gt;&lt;/ul&gt;
&lt;p&gt;Every one of those needs logs. An AI agent that can list your APIs but can&#x27;t read a stack trace can&#x27;t help with any of them. So mulewatch does one job — observability — and stays out of deployment entirely.&lt;/p&gt;
&lt;h2 id=&quot;why-isnt-the-runtime-manager-log-tail-enough&quot;&gt;Why isn&#x27;t the Runtime Manager log tail enough?&lt;/h2&gt;
&lt;p&gt;The logs you see in Runtime Manager are a &lt;strong&gt;live tail&lt;/strong&gt;: a small rolling buffer per application. On a busy app it can scroll out within minutes, whatever &lt;code&gt;startTime&lt;/code&gt; and &lt;code&gt;endTime&lt;/code&gt; you pass. If the incident happened last night, the tail has already forgotten it.&lt;/p&gt;
&lt;p&gt;The history lives somewhere else: the &lt;strong&gt;Anypoint Monitoring Archive API&lt;/strong&gt;. It stores log files in ten-minute windows, landing each file roughly ten minutes after its window closes, and keeps them far longer. It needs Anypoint Monitoring to be enabled for the organization.&lt;/p&gt;
&lt;h2 id=&quot;how-does-the-monitoring-archive-index-logs&quot;&gt;How does the Monitoring Archive index logs?&lt;/h2&gt;
&lt;p&gt;This is the part that makes archive search slow if you do it naively. The archive is indexed &lt;strong&gt;per replica&lt;/strong&gt;, not per application. Each entity is named &lt;code&gt;{appName}_{replicaId}&lt;/code&gt;, and every redeploy creates new replicas. You cannot ask for &quot;the logs of &lt;code&gt;order-sync-api&lt;/code&gt; on 9 August&quot;; you have to know which replica was running that day and ask for its files.&lt;/p&gt;
&lt;p&gt;An application that has been redeployed often accumulates hundreds of replica entities. Probing each one is slow, and the Archive API allows &lt;strong&gt;60 requests per minute&lt;/strong&gt;.&lt;/p&gt;
&lt;h2 id=&quot;how-does-mulewatch-find-the-right-replica-quickly&quot;&gt;How does mulewatch find the right replica quickly?&lt;/h2&gt;
&lt;p&gt;It takes two paths and reports which one it used:&lt;/p&gt;
&lt;ol&gt;&lt;li&gt;&lt;strong&gt;Live-replica fast path.&lt;/strong&gt; It reads the replica ids visible in the current Runtime Manager tail and tries those first. Pods usually live between redeploys, so the replica running now normally produced the logs of the recent past too. A typical lookup costs a handful of archive calls.&lt;/li&gt;&lt;li&gt;&lt;strong&gt;Full-scan fallback.&lt;/strong&gt; If that replica has no files for the requested dates — because the app was redeployed since — it lists every replica the application has ever had and probes each one, bounded by a &lt;code&gt;maxEntities&lt;/code&gt; cap.&lt;/li&gt;&lt;/ol&gt;
&lt;p&gt;On a production application used for testing, the archive held &lt;strong&gt;669 replica entities&lt;/strong&gt;. A search for the current day went through the fast path, probed &lt;strong&gt;one&lt;/strong&gt; entity, found 127 archive files and parsed 2,859 log lines. A date from before the last redeploy correctly fell back to the full scan, which is the slow case the cap exists for.&lt;/p&gt;
&lt;p&gt;The client throttles itself below the 60 requests/minute limit and backs off on &lt;code&gt;429&lt;/code&gt;, so a long search slows down instead of failing.&lt;/p&gt;
&lt;h2 id=&quot;why-is-every-tool-read-only&quot;&gt;Why is every tool read-only?&lt;/h2&gt;
&lt;p&gt;Because an LLM should not be restarting production Mule applications, and the official server already covers deployment for people who want it. mulewatch has no deploy, stop, restart or policy tools at all.&lt;/p&gt;
&lt;p&gt;It also scopes the whole server, not one tool, to the environments you allow:&lt;/p&gt;
&lt;pre data-lang=&quot;dotenv&quot;&gt;&lt;code&gt;ANYPOINT_ALLOWED_ENVIRONMENTS=Dev,Sandbox&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;With that set, every tool refuses anything outside the list and says why. Pair it with a connected app that only has access to those environments and production is out of reach twice over.&lt;/p&gt;
&lt;h2 id=&quot;what-trips-people-up-when-connecting-to-anypoint&quot;&gt;What trips people up when connecting to Anypoint?&lt;/h2&gt;
&lt;p&gt;One thing, almost every time: &lt;strong&gt;environments belong to business groups, not to the root organization.&lt;/strong&gt; Connected-app credentials resolve to the root org, which often has zero environments — so the first call comes back empty and it looks like the credentials are wrong.&lt;/p&gt;
&lt;p&gt;mulewatch has an &lt;code&gt;anypoint_list_business_groups&lt;/code&gt; tool for exactly this. List the groups, then pass the right id as &lt;code&gt;orgId&lt;/code&gt; or pin it with &lt;code&gt;ANYPOINT_ORG_ID&lt;/code&gt;.&lt;/p&gt;
&lt;p&gt;The second trap is scopes. Anypoint answers a missing scope with a bare &lt;code&gt;403 Forbidden&lt;/code&gt;; mulewatch catches it and names the scope you probably need (&lt;code&gt;Read Applications&lt;/code&gt;, Monitoring &lt;code&gt;Viewer&lt;/code&gt;, &lt;code&gt;View APIs Configuration&lt;/code&gt;…).&lt;/p&gt;
&lt;h2 id=&quot;how-do-i-try-it&quot;&gt;How do I try it?&lt;/h2&gt;
&lt;p&gt;Create a connected app in &lt;strong&gt;Access Management → Connected Apps&lt;/strong&gt; (&quot;acts on its own behalf&quot;), grant the read scopes, and point your MCP client at &lt;code&gt;npx&lt;/code&gt;:&lt;/p&gt;
&lt;pre data-lang=&quot;json&quot;&gt;&lt;code&gt;{
  &quot;mcpServers&quot;: {
    &quot;mulewatch&quot;: {
      &quot;command&quot;: &quot;npx&quot;,
      &quot;args&quot;: [&quot;-y&quot;, &quot;mulewatch&quot;],
      &quot;env&quot;: {
        &quot;ANYPOINT_CLIENT_ID&quot;: &quot;your_connected_app_client_id&quot;,
        &quot;ANYPOINT_CLIENT_SECRET&quot;: &quot;your_connected_app_client_secret&quot;
      }
    }
  }
}&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;For Claude Code:&lt;/p&gt;
&lt;pre data-lang=&quot;bash&quot;&gt;&lt;code&gt;claude mcp add mulewatch --env ANYPOINT_CLIENT_ID=... --env ANYPOINT_CLIENT_SECRET=... -- npx -y mulewatch&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;Start with &lt;code&gt;anypoint_whoami&lt;/code&gt;: it shows which credentials, business group and environment allowlist the server is running with. Then ask in plain language — &lt;em&gt;&quot;show me the ERROR lines of order-sync-api in Prod yesterday between 1 and 3 a.m.&quot;&lt;/em&gt; — and the agent picks &lt;code&gt;anypoint_search_archived_logs&lt;/code&gt;.&lt;/p&gt;
&lt;h2 id=&quot;whats-the-current-status&quot;&gt;What&#x27;s the current status?&lt;/h2&gt;
&lt;p&gt;Version 0.1, Apache-2.0, on &lt;a href=&quot;https://www.npmjs.com/package/mulewatch&quot; target=&quot;_blank&quot; rel=&quot;noopener noreferrer&quot;&gt;npm&lt;/a&gt; and the &lt;a href=&quot;https://registry.modelcontextprotocol.io/?q=mulewatch&quot; target=&quot;_blank&quot; rel=&quot;noopener noreferrer&quot;&gt;MCP registry&lt;/a&gt;. Every read path was exercised against a live Anypoint organization, except API Manager instances, which still need a test with the &lt;code&gt;View APIs Configuration&lt;/code&gt; scope. Issues and pull requests are welcome on &lt;a href=&quot;https://github.com/brbousnguar/mulewatch&quot; target=&quot;_blank&quot; rel=&quot;noopener noreferrer&quot;&gt;GitHub&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;&lt;em&gt;MuleSoft, Anypoint Platform and CloudHub are trademarks of Salesforce, Inc. mulewatch is an independent project, not affiliated with or endorsed by Salesforce.&lt;/em&gt;&lt;/p&gt;</content>
  </entry>
</feed>
