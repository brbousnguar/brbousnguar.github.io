### Changed
- English and French now have their own URLs — `/` and `/pages/about.html` in English, `/fr/` and `/fr/a-propos.html` in French — each with one language, one `<h1>`, French titles/descriptions/share card and reciprocal `hreflang`; the sitemap lists both with alternates (#43)

### Removed
- The in-page language toggle, `assets/js/main.js` and the `localStorage` language preference; the EN/FR switch is now two links (#43)
