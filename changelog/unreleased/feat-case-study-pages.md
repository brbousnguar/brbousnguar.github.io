### Added
- Anonymised case-study pages in English (`/work/`) and French (`/fr/missions/`) for the three client missions, each with the integration diagram, the mission facts, what was built and where it landed; `Article` JSON-LD, hreflang pairs, listed in `llms.txt` / `llms-full.txt`; the home case cards link to them (#45)

### Changed
- `tools/build_site.py`: project and case-study pages share one `detail_page()` template (#45)
