### Added
- `/notes/` writing section (English-only): Markdown sources in `notes/`, built by the dependency-free `tools/build_notes.py` into note pages with TechArticle JSON-LD, a notes index, an Atom feed (`/notes/feed.xml`), `llms-full.txt`, the note list in `llms.txt`, and a generated `sitemap.xml`; each note's `.md` is published for AI agents (#44)
- First note: "The official MuleSoft MCP server can't read your logs, so I built one that can" (#44)
- "Notes" in the header nav and feed discovery links on every page (#44)
