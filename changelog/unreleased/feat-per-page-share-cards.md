### Added
- A share card per note, project and case-study page (EN and FR), rendered by `tools/make_og_card.py` into `assets/img/og/` and picked up by `tools/build_site.py`, so link previews show the page's own title (#60)
