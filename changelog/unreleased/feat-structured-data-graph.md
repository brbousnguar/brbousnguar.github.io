### Changed
- Structured data is now one linked JSON-LD `@graph` per page — `Person` (`#person`) with image, Nantes address, languages and occupation, `WebSite`, `ProfilePage` / `AboutPage` and breadcrumbs — so search and AI answer engines resolve the site to a single entity (#41)
