### Changed
- Fonts are self-hosted from `assets/fonts/` (latin woff2, OFL licences included) with the two variable fonts preloaded, replacing the render-blocking Google Fonts stylesheet on every page; rendering is pixel-identical (#68)
